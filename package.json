{
  "name": "mockly",
  "author": "Ievgen Golovachov",
  "description": "Easily generate and manage mock data for testing and development",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd mockly-backend && npm install",
    "install:frontend": "cd mockly-frontend && npm install",
    "start:backend": "cd mockly-backend && npm run start:dev",
    "start:frontend": "cd mockly-frontend && npm run dev",
    "test:backend": "cd mockly-backend && npm test",
    "build:frontend": "cd mockly-frontend && npm run build"
  }
}